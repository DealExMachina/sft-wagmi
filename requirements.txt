# Unsloth SFT — Wagmi / Qwen2.5-1.5B-Instruct
# Install on a CUDA 12.4 + PyTorch 2.6 host (e.g. HuggingFace Space)
# Run: pip install -r requirements.txt
# If you see torchao / register_constant errors, reinstall torch after (see Dockerfile).

# Core Unsloth wheel — picks the right CUDA variant automatically
# unsloth_zoo: required import metadata (unsloth __init__ calls importlib.version); keep explicit.
unsloth_zoo
unsloth @ git+https://github.com/unslothai/unsloth.git

# Training stack (torch may be downgraded by unsloth; Dockerfile reinstalls cu124 torch after)
torch>=2.6.0
trl>=0.12.0
transformers>=4.47.0
datasets>=3.0.0
peft>=0.14.0
bitsandbytes>=0.45.0
accelerate>=0.34.0

# Hub + logging
huggingface_hub>=0.26.0
wandb>=0.19.0

# Gradio (Space entrypoint: app.py)
gradio>=4.44.0

# Autotune judge (OpenAI GPT-4o)
openai>=1.0.0

# Utilities
sentencepiece>=0.2.0
protobuf>=4.25.0
packaging>=24.0
