# Unsloth SFT — Wagmi / Qwen2.5-1.5B-Instruct + auth (Mistral Small 3.1 via Unsloth)
#
# Stack alignment (check when upgrading):
# - Dockerfile: pytorch 2.7.1 + CUDA 12.6 + post-install torch from cu126 index (torchao needs
#   torch.utils._pytree.register_constant — present from PyTorch 2.7 onward, not in 2.6.x).
# - Unsloth upstream optional deps: see https://github.com/unslothai/unsloth/blob/main/pyproject.toml
#   [project.optional-dependencies] huggingfacenotorch / huggingface.
# - Mistral Small 3.1 model card (mistralai/…): recommends vLLM; marks Transformers as lightly tested.
#   We use unsloth/Mistral-Small-3.1-24B-Instruct-2503 (Unsloth’s packaging) + this TRL/Unsloth stack.
#
# Run: pip install -r requirements.txt
# If you see torchao / register_constant errors, see Dockerfile (torch reinstall order).

# Core Unsloth (git) + zoo (PyPI; version aligned with unsloth[huggingface] extra)
unsloth_zoo>=2026.4.2
unsloth @ git+https://github.com/unslothai/unsloth.git

# PyTorch: Dockerfile pins torch==2.7.1+cu126 (register_constant + unsloth_zoo requires torch<2.11).
torch>=2.7.0,<2.11.0

# Hugging Face training stack — floors match unsloth huggingfacenotorch (approx.; bad transformers
# micro-releases are excluded upstream — bump carefully if Unsloth CI breaks)
trl>=0.18.2,<=0.24.0
transformers>=4.51.3,<=5.5.0
datasets>=3.4.1,<4.4.0
peft>=0.18.0
bitsandbytes>=0.45.5
accelerate>=0.34.1

# Hub + logging
huggingface_hub>=0.26.0
wandb>=0.19.0

# Gradio (Space entrypoint: app.py)
gradio>=4.44.0

# Autotune judge (OpenAI GPT-4o)
openai>=1.0.0

# Utilities
sentencepiece>=0.2.0
protobuf>=4.25.0
packaging>=24.0
